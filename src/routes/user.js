const express = require('express');
const { retrieveUserRecord } = require('../firebase');

const router = express.Router();

router.get('/', async function(req, res, next) {
  try {
  } catch (error) {}
});

module.exports = router;
